# Styles for pdf2md2

This directory contains CSS styles used when converting Markdown to PDF.

- `included/`: CSS files packaged with pdf2md2.
- `cache/`: Download cache for CSS fetched from URLs via the `-s/--style` option.

To refresh or add more styles to `included/`, see scripts in `scripts/`.
